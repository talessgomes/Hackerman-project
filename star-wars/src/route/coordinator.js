export const gotoLogin = (history) => {
    history.push("/")
}

export const goToCharacter = (history) => {
    history.push("/Personagem")
}

export const goToInformation = (history) => {
    history.push("/Informação-Personagem")
}